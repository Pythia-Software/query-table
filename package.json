{
  "name": "query-table-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Schema-driven data table — core, react, and ui packages.",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "tools/*",
    "demo"
  ],
  "scripts": {
    "build:core": "tsc -p packages/core",
    "build": "npm run build:core && tsc -p packages/react && tsc -p packages/ui",
    "lint": "npm run typecheck",
    "typecheck": "npm run build:core && tsc --noEmit -p packages/core/tsconfig.test.json && tsc --noEmit -p packages/react/tsconfig.test.json && tsc --noEmit -p packages/ui",
    "test": "vitest run",
    "demo": "npm run dev --workspace @query-table/demo",
    "test:dnd": "node demo/dnd-test.mjs",
    "prepare": "husky"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^20.11.30",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.7",
    "ajv": "^8.20.0",
    "husky": "^9.1.7",
    "jsdom": "^29.1.1",
    "playwright": "^1.60.0",
    "react": "^18.3.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
